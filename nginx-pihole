# Install Nginx

# Download packages
https://nginx.org/en/linux_packages.html
sudo apt install curl gnupg2 ca-certificates lsb-release

# Set up the apt repository for stable packages
echo "deb http://nginx.org/packages/ubuntu `lsb_release -cs` nginx" \
      | sudo tee /etc/apt/sources.list.d/nginx.list

# Import official nginx signing key for verifcation of packages
curl -fsSL https://nginx.org/keys/nginx_signing.key | sudo apt-key add -
sudo apt-key fingerprint ABF5BD827BD9BF62

# The output should contain the full fingerprint 573B FD6B 3D8F BC64 1079 A6AB ABF5 BD82 7BD9 BF62

sudo apt update
sudo apt install nginx

# Configuration File
sudo vim /etc/nginx/nginx.conf

# sites-available for troubleshooting
sudo vim /etc/nginx/sites-available/default

# Starting, stopping, reloading
sudo systemctl [ start | restart | status ] nginx


# Use Nginx instead of Lighttpd as the webserver

Basic requirements

1.    Stop default lighttpd
2.    service lighttpd stop
3.    Install necessary packages
4.    apt-get -y install nginx php7.0-fpm php7.0-zip apache2-utils
4.    Disable lighttpd at startup
5.    systemctl disable lighttpd
6.    Enable php7.0-fpm at startup
7.    systemctl enable php7.0-fpm
8.    Enable nginx at startup
9.    systemctl enable nginx
10.   Edit /etc/nginx/sites-available/default to:

# Line 4 did install php7.0 here is the solution

sudo apt-add-repository ppa:ondrej/php
sudo apt-get update
sudo apt-get install php7.0

# Before step 6 worked

sudo apt-cache search php7.0- |more
sudo apt-get install php7.0-fpm

# Nginx
on raspi server, edit /etc/nginx/sites-available/default config file to listen for port 80 and redirect for https

# Create symlink between sites-available/default to sites-enabled
sudo ln -s /etc/nginx/sites-available/default /etc/nginx/sites-enabled/






